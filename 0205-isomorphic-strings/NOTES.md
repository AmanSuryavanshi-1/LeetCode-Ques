```
(mapST.find(c1) != mapST.end() && mapST[c1] != c2)
​
mapST.find(c1) != mapST.end()
If c1 is found in mapST
​
&& mapST[c1] != c2
If c1 is already mapped to a character in t, this part checks if the mapped character is not equal to c2 from string t
```
​